Day-4-Frontend-Dashboard/
│
├── backend/
│   └── main.py
│
├── index.html
│
└── README.mdBackend Codebackend/main.py (FastAPI WebSocket for live price simulation every 2 seconds):from fastapi import FastAPI, WebSocket
from fastapi.middleware.cors import CORSMiddleware
import random
import asyncio
from datetime import datetime

app = FastAPI()

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.websocket("/ws")
async def websocket_endpoint(websocket: WebSocket):
    await websocket.accept()

    price = 100.0

    while True:
        change = random.uniform(-2, 2)
        price += change

        data = {
            "price": round(price, 2),
            "time": datetime.now().strftime("%H:%M:%S")
        }

        await websocket.send_json(data)
        await asyncio.sleep(2)Run with: uvicorn backend.main:app --reload (access http://127.0.0.1:8000) �.Frontend Codeindex.html (Complete dashboard with charts, stats, time display, and animations):<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Photon Stock Dashboard</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<style>
body {
    margin: 0;
    background: #121212;
    color: white;
    font-family: Arial, sans-serif;
}

header {
    text-align: center;
    padding: 10px;
}

#time {
    font-size: 14px;
    opacity: 0.8;
}

#dashboard {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 10px;
    padding: 10px;
}

#stats, #ticker, .chart-box {
    background: #1f1f1f;
    border-radius: 8px;
    padding: 10px;
}

#ticker {
    overflow: hidden;
    white-space: nowrap;
    font-weight: bold;
}

.stat {
    margin-bottom: 8px;
}

.up { color: #4caf50; }
.down { color: #f44336; }

.chart-box {
    height: 220px;
}
</style>
</head>

<body>

<header>
    <h2>📈 Photon Stock Dashboard</h2>
    <div id="time"></div>
</header>

<div id="dashboard">

    <div>
        <div id="stats">
            <div class="stat">Current Price: <span id="price">--</span></div>
            <div class="stat">Change: <span id="change">--</span></div>
        </div>
        <div id="ticker">Waiting for data...</div>
    </div>

    <div>
        <div class="chart-box">
            <canvas id="priceChart"></canvas>
        </div>
        <div class="chart-box">
            <canvas id="volumeChart"></canvas>
        </div>
    </div>

</div>

<script>
/* Date & Time */
setInterval(() => {
    document.getElementById("time").innerText =
        new Date().toLocaleString();
}, 1000);

/* Charts */
const priceCtx = document.getElementById("priceChart");
const volumeCtx = document.getElementById("volumeChart");

const priceChart = new Chart(priceCtx, {
    type: "line",
    data: {
        labels: [],
        datasets: [{
            label: "Price",
            data: [],
            borderColor: "#ff9800",
            tension: 0.4
        }]
    },
    options: {
        responsive: true,
        animation: false,
        plugins: { legend: { display: false } }
    }
});

const volumeChart = new Chart(volumeCtx, {
    type: "bar",
    data: {
        labels: [],
        datasets: [{
            label: "Volume",
            data: [],
            backgroundColor: "#2196f3"
        }]
    },
    options: {
        responsive: true,
        animation: false,
        plugins: { legend: { display: false } }
    }
});

/* WebSocket */
const ws = new WebSocket("ws://127.0.0.1:8000/ws");

let lastPrice = null;

ws.onmessage = (event) => {
    const data = JSON.parse(event.data);
    const price = data.price;
    const time = data.time;

    let change = lastPrice ? (price - lastPrice).toFixed(2) : 0;

    document.getElementById("price").innerText = "₹ " + price;
    document.getElementById("change").innerText = change;

    document.getElementById("change").className =
        change >= 0 ? "up" : "down";

    const ticker = document.getElementById("ticker");
    ticker.innerText = `PHOTON ₹${price} (${change}) at ${time}`;

    priceChart.data.labels.push(time);
    priceChart.data.datasets[0].data.push(price);

    volumeChart.data.labels.push(time);
    volumeChart.data.datasets[0].data.push(
        Math.floor(Math.random() * 800 + 200)
    );

    if (priceChart.data.labels.length > 15) {
        priceChart.data.labels.shift();
        priceChart.data.datasets[0].data.shift();
        volumeChart.data.labels.shift();
        volumeChart.data.datasets[0].data.shift();
    }

    priceChart.update();
    volumeChart.update();

    lastPrice = price;
};
</script>

</body>
</html>README.md Content# DAY 4 – Frontend Dashboard Enhancements

This day focuses on improving the frontend of the Photon Stock Dashboard.

## Features implemented:
- Live stock updates using WebSocket
- Line chart for stock price
- Bar chart for volume
- Date and time display
- Stats panel for price and change
- Clean and stable dashboard layout

## Technologies:
HTML, CSS, JavaScript, Chart.js, FastAPI, WebSocket

## Setup:
1. Run backend: `uvicorn backend.main:app --reload`
2. Open `index.html` in browser
